> **Состояние на 2026-09-02, дописано при коммите файла.** Ниже — задание как оно было
> написано; текст автора не тронут. Часть его фактов с тех пор устарела, поэтому сверка:
>
> | пункт задания | состояние |
> |---|---|
> | «сейчас это v4.7.2-man» | устарело: ручная линия — `v4.7.5-man` (свод пяти дек 4.7.x), сборка — **v4.10.0**, 136 слайдов |
> | порядок слайдов в начале (AE / ELT vs ETL) | **перенесено в контент**: ELT → история dbt → «dbt за 60 секунд» → Viewpoint → AE |
> | изменённые слайды, новые и заменённые картинки | **перенесено**: 4 картинки в медиатеке, вытесненные помечены `manual-removed` в реестре |
> | слайд про Data Mesh | **есть в деке**, слайд 86 `061h2-data-mesh-domeny-i-kontrakty`. Акцент «самостоятельная работа в команде MLE» отдельно НЕ проверялся |
> | сдвинуть «MLOps-смысл ключевых концепций dbt» после самих концепций | **не сделано** — слайд 17, то есть по-прежнему до них |
> | «Shift left» — в блок CI и детализировать | **не сделано** — слайд 24, блок CI начинается со слайда 76 |
> | сложные MLE-слайды — после Airflow, CI/CD, Data Mesh и AI | **частично**: PART 2 «DS/MLE нюансы» стоит со слайда 92, то есть после всего перечисленного |
>
> Направление слияния при этом обратное тому, что здесь описано: правки переносились
> **из ручной деки в контент** (`content/preza-dbt-v4-content.yml`), а не наоборот —
> генератор пересобирает `.pptx` из YAML, и правка, оставшаяся только в файле, умирает на
> следующей сборке. См. [`docs/deck-manual-pass.md`](../docs/deck-manual-pass.md).

---

Смержи в мою версию MLInside_Введение-в-dbt_v4.7.{last-merged-all-mans}-man с ручными правками доработки из MLInside_Введение-в-dbt_v4.8.0+ (или/и более поздние, если уже появились после этой), обязательно сохранив следующие мои следующие доработки:
- я поменял порядок слайдов в начале (где про AE и ETL vs ETL)
- довольно сильно поменял некоторые слайды, в тч добавив на них новые картинки и удалив (заменив) некоторые старые

+ Плюс обязательно добавь про слайд(ы) про
- Data mesh (где то уже сгенерён – найди) + с акцентом на самостоятельную работу с проектом в команде MLE

Переменсти слайд на нужное место, насколько это возможно в рамках этой презентации
- "MLOps-смысл ключевых концепций dbt" – после объяснения всех (или хотя бы большинства) этих концепций
- "Shift left: ловим ошибку до CI" куда-то в CI может быть? (+ дополни/детализируй его – пока он как-то вырван из контекста)
- сложные MLE-слайды переместим после сравнения с Airflow и CI/CD, Data mesh и использования AI совместно с dbt


+ если меняешь саму презентацию, то убедить, что работает с последней/текущей версией презентации – сейчас это MLInside_Введение-в-dbt_v4.7.2-man (но каждый раз уточняй это)
