
## 2026-07-19 — Librarian
- Review paths (Downloads/HSU & MLInside, MLInside_*.pptx, _2025-11-ВШЭ_ВНЕШНЕЕ_ОБУЧЕНИЕ): remove doubles, sort by categories into ./data/, version stacks → curr + .history/, git LFS, CATALOG.md (deterministic doc-props + reviews), propose data/ scaffold; implement as "Librarian" submodule.
- Also move assets/ → data/.
- Answers: Move (not copy) from iCloud; full scope (decks+docs+videos+Downloads dbt+ДЗ repos as submodules); hnkovr/librarian; dedupe during assets move.
- /reask, /finalize-issue

## 2026-07-20 — Deck generator skill + 3 DE-tool decks
- Generate a Dagster preza: context/history/persons/present; architecture + all entities (assets, jobs, sensors, backfills, locations); dev vs prod + dagster.io comparison; advanced dbt integration incl. multi-dbt; Airflow+Cosmos comparison; SQLMesh; dlt & Airbyte; CLI & MCP; Prefect acquisition + perspectives; competitors; multi-orchestrator (airflow/prefect/kestra/argo/mage); homework from the dbt HW. More code schemes/examples.
- Create all needed sub-skills + meta-skill `create-preza-about-de-tool`.
- Ensure 20–50 slides (as a setting); model/harness/effort/version stamp in first+last speaker notes; very detailed speaker text.
- Port the CC skill if it exists, else create it; then run it for: "Современная оркестрация ML-пайплайнов (Dagster)", "… (Prefect)", "Автоматизация и мониторинг (CI/CD + Prometheus/Grafana)" (glab & gh + comparison, VM runners, AI trends; observability legacy→modern incl. ELK/Loki/Victoria; Telegram/Mattermost alerting; AI for Observability; MLOps/LLMOps).
- Answers: implement all 3 visual profiles; skill + all 3 decks in one pass; canonical via /create-skill + project-local port via BOTH symlink & hardlink (with notes inside), maximizing deterministic scripts + code standards.
- /create-skill, /smart-commit

## 2026-07-20 — Apache Airflow lecture deck

**Prompt:** «сделай презу по Оркестрация данных (Apache Airflow)» — управление
процессами (Scheduler/Worker/Webserver, DAG и операторы), расписание и ETL
(регулярный запуск dbt-моделей, сбор сырых данных, батч-прогнозы), передача данных
(ограничения XCom, метаданные вместо тяжёлых датафреймов), обработка сбоев
(retries и алерты).

**Сделано:** `create-preza-about-de-tool "Оркестрация данных (Apache Airflow)"` →
`content/preza-apache-airflow-content.yml` (40 слайдов, профиль `code-tables`,
провенанс-штамп в заметках первого и последнего слайда); зарегистрирована в
`settings/config.yml → deck_generation.decks`, `just airflow-build` +
`preza-validate-all`; сборка `MLInside_Оркестрация-данных-Apache-Airflow_v1.1`.

## 2026-08-11 — workstation bootstrap (commit+push всё, secrets sync, skills/agents/hooks)

**Prompt:** «find, commit and push all git-worktrees of curr repo & maybe other manually
cloned this repo local worktrees; add using bitwarden-cli or/and git-secret to supply
secrets sync to new workstation; /goal — the repo should be whole committed and prepared
to farther r&d on new workstation; upsert skills, role sub-agents and hooks 4 this».

**Сделано:** инвентарь (1 worktree ahead-16+dirty, 6 сабмодулей и hw-olist-клон в синхроне);
ремонт origin-доступа (re-invite hnkovr в приватный dataengy/MLInside-course); 5+2
категорийных коммитов + ретро-биндинг 16 июльских к GH Issues #2–#5 (зонт #1); push
57460a4..442ff4a; secrets-lane bw+git-secret (scripts/secrets-sync.sh, just secrets-*,
runbook, .secret-блоб); агент workstation-bootstrapper; SessionStart-хук repo-sync-status;
кандидаты ~/.ai: commit-push-repo-family (новый), secrets-sync-workstation (Learnings-апсерт).

## 2026-08-13..16 — ревью трёх дек по тезисам GSheet + акцентная лента

**Prompt:** «Review my presentations (dbt, Dagster, Observability & CI/CD) with main GSheet
reqs 4 them, and extend/update them with focus on those reqs»; mid-turn: «upsert
memory/docs/specs/scripts/skills/skills-settings/role-sub-agents/hooks about this (also not
in this repo! outside it in some ~/.ai/ places)»; далее «go on» ×4 (ADC-консент не добит →
фолбэк-транспорт разрешён).

**Сделано:** лента листа поднята целиком — settings/gsheet.yml (полная карта колонок,
«тезисы»→accents), mapper.split_accents + accents_split_numbering (+юнит-тест), dbt-topic
переименован под «название» листа; дамп через claude.ai Drive-коннектор (формат fetch_raw,
помечен; ADC-лента остаётся — adc-login слушает), `python -m schedule plan`: 4 updated /
4 added / 3 stale. Точечные правки дек: dbt 52 слайда (052-sloi-dbt-proekta + 3 буллета),
Dagster 51 (051-ml-pajplajn + 3 правки, штамп v1.1 fable-5), CI/CD 42 (051-docker-в-CI,
052-fastapi-instrumentaciya + 2 буллета, штамп v1.1); slides_max 55. preza-review-all:
**12/12 hit, 0 err/warn** — закрыта .ai/tasks/0006. Сборка v3.14/v1.3/v1.3; 21 тест зелёный.
Обвязка: хук preza-accents-status.sh, агент preza-accents-keeper (scope: project), память
schedule_gsheet_accents_lane, ~/.ai/projects/MLInside-course/, скилл preza-review дополнен
синк-лентой и заново захардлинкен, reset-google-account-creds: post_reset-лейн.
Docs: docs/schedule-gsheet-lane.md, CHANGELOG 2026-08-16.

## 2026-08-16..18 — Deck publish pipeline (TG + GDrive + GSheet columns)

**Prompt:** «upsert hook/pipeline on new version presentation generation — invoke
tg-informet and tg-sender-last-versions; share generated pptx to GDrive with persistent
name/location/URL per subject; extend GSheet with columns: persistent URL / version /
slides». Answers: транспорт — сначала OAuth-токен AGD-gen (умер, invalid_grant) → ADC с
write-скоупами; триггер — явный just publish-new + наг-хук; Drive — 1 файл на предмет
без версии в имени.

**Сделано:** src/publisher/ (detect/state/settings/auth/gdrive/gsheet_write/telegram_leg/
plan_writer/runner/cli) + settings/publish.yml + Justfile publish-new{,-dry}/status/
init-drive (send/build-send удалены) + SessionStart-хук deck-publish-status.sh + 43
теста (suite 251). Курсор + published:-блок (кросс-машинный сид). Коммит dcad8e6 (#6).
Side-fix: adc-login account=-баг (hnkovr/.ai#8, de2c45b), write-скоупы в дефолтах
reset-google-account-creds. Живой прогон ждёт браузерного ADC-консента (чек-лист в
docs/deck-publish-pipeline.md).

---

## 2026-08-20..22 — «go on» ×2 · авторизация Google-записи

**Промпты:** повторный запуск `adc-login` (пришёл текстом из-за ведущих пробелов — запустил
сам), «This app is blocked» на консенте, ответ на вопрос про аккаунт Drive: «Use hnkovr,
space is not a problem (I'll increase it)», затем «go on».

**Сделано:** найдено, что блокировал **sensitive `spreadsheets`**, а не только restricted
`drive` — тот же логин без него прошёл. Отсюда две ленты кредлов (`PublishConfig.lane`):
Drive ← user-ADC (`drive.file`, quota-проект на кредле через `with_quota_project` —
машинный ADC-файл не трогаем), лист ← сервис-аккаунт `gsheets-reader@…`. Папка Drive
создана (`drive.folder_id`), первый аплоад упал на `storageQuotaExceeded` (98.70/15 GiB —
место, не права). Хук `deck-publish-status.sh` перестал сваливать в одну строку «собрано
новее» и «леги не завершены» (`db8fc69`). Добавлен `publisher/errors.py` — отказ переводится
в действие; `.tmp/probe_google_access.py` переписан в проверку ворот + репетицию записи
листа по живым данным (таб Sheet1, тема в B, колонки в E/F/G, строки 5/6/7/9).
Коммиты: `db8fc69`, `b0cac14`, `602953b`, `978f677` (#6). Тесты 248 passed, 4 skipped.

---

## 2026-08-24 — «Send last prezas versions to tg»

**Промпты:** «Send last prezas versions to tg» ×2. На вопрос, что делать (все шесть
актуальных версий уже ушли в TG 2026-08-18, повтор возможен только `--force`) — ответ
«#4: сначала пересобрать, потом слать» + два контентных изменения: «main dbt packages
describing» и «move SQLMesh slide from Dagster preza into dbt preza». На уточнение по
объёму: пакеты — «#1 + #3» (расширить таблицу **и** добавить блок слайдов), SQLMesh —
оба слайда.

**Сделано:** dbt-дека 52 → 57 слайдов. Таблица `032-klyuchevye-pakety` получила третью
колонку «Когда брать» и строку `dbt_codegen` (8 пакетов). Новый блок: `053-dq-pakety-…`
(dbt_expectations + elementary — инвариант против аномалии), `054-audit-helper-…`
(`compare_relations` из `analyses/`, project_evaluator), `055-pakety-versii-i-riski`
(пины версий, package-lock.yml, общее пространство имён макросов). Из Dagster-деки
переехали два SQLMesh-слайда (`033-sqlmesh-alternativa-dbt` → `056-…`,
`034-dbt-protiv-sqlmesh` → `057-…`) — в dbt-деке они встали вокруг существующего
`045-sqlmesh-i-dbt-upravlenie`: что это → как меняет workflow → вердикт. Dagster 51 → 49,
буллет плана «SQLMesh, dlt и Airbyte» → «dlt и Airbyte». `slides_max` 55 → 58
(settings/config.yml). Оба ревью 4/4 акцента, 0 ошибок; тесты 248 passed, 4 skipped
(пины 52→57 и 14→18 code-слайдов в src/tests/test_content.py).

**Отправлено в TG** (топик 118): `MLInside_Введение-в-dbt_v3.15` (57 слайдов) и
`MLInside_Современная-оркестрация-ML-пайплайнов-Dagster_v1.4` (49) — `just publish-new
--only tg`, остальные четыре деки не тронуты («already ok»). Леги drive+sheet по-прежнему
ждут внешних условий (квота Drive, Editor для SA).

---

## 2026-08-25 — «extend dbt packages content» + сжать SQLMesh, добавить SWOT и SQLMesh-over-dbt

**Промпты:** «open the dbt deck»; «extend dbt packages content»; «compact to 1 (max 2) slides
about SQLMesh & add SWOT dbt-vs-SQLMesh, SQLMesh-over-dbt setup». На уточнение по объёму
пакетов — «ещё пакеты под задачу» + «пакеты в CI и проде» и отдельным списком: тулинг
(VS Code + плагины, специализированная IDE, «SQLBuild»), MCP, skills, slim-CI, список
пакетов, dbtCloud/fusion, dbt Core v2.0 (State, Wizard). Два имени уточнены отдельным
вопросом: «SQLBuild» → SqlDBM + sqlfluff/sqlfmt, IDE → Paradime + dbt Studio.

**Сделано:** dbt-дека 57 → 62 слайда. SQLMesh сжат с трёх слайдов до двух:
`063-sqlmesh-i-dbt-swot` (SWOT-квадранты вместо трёх таблиц сравнения) и
`064-sqlmesh-poverh-dbt-proekta` (`sqlmesh init -t dbt` по живой документации: что читается,
что становится audits, чего не поддержано). Новое: `058-gde-pisat-dbt` (CLI · Power User ·
официальное расширение на Fusion · браузерные IDE · SqlDBM), `059-fusion-i-dbt-core-v2`,
`060-esche-pakety-pod-zadachu` (dbt_profiler, dbt_external_tables, dbt_artifacts, dbt-score,
dbt-colibri — с пометкой, что последние два ставятся pip-ом, а не через packages.yml),
`061-pakety-v-ci-i-prode`, `062-slim-ci-state-modified`, `065-dbt-state-i-dbt-wizard`.
Расширены `039` (sqlfmt рядом с SQLFluff: сначала формат, потом правила) и `044` (MCP в IDE,
агентные скиллы). Исправлена атрибуция `dbt_expectations`: calogica заброшен 2024-12-18 →
`metaplane`. `slides_max` 58 → 64.

**Грабли:** буллеты с двоеточием без кавычек YAML разбирает как мапы — валидатор и
`preza-review` это пропускают, ловит только билд (`TypeError: got 'dict'`). Четыре штуки
закавычены.

**Отправлено в TG:** `MLInside_Введение-в-dbt_v3.18` (62 слайда) в топик 118.

**Чужое в дереве:** `src/publisher/gapi.py` (untracked) + правки `gdrive.py`/`gsheet_write.py`
от параллельной сессии; её doctest `set_retries` красный. Не коммитил, коммиты
path-limited.

---

## 2026-08-25 (2) — моделирование, MV/стриминг/права + upsert инструментов

**Промпты:** «add to dbt (shortly?)» со списком: основы моделирования (ER → генерация dbt,
medallion, EDW-слои rnd/raw→stg→core→dds→ods→rep/bi+fs/ad-hoc, DV + AutomateDV), MV на
примерах ClickHouse, стриминг (Spark Structured Streaming / RisingWave), микробатчинг,
управление правами в dbt и СУБД. Затем в ходе работы: «add dbt_project_evaluator» и
«upsert .tmp/, scripts, skills, subagents».

**Дека:** 62 → 70 слайдов. Новое: `066-modelirovanie-ot-er-k-dbt`, `067-medallion-i-edw-sloi`
(таблица сопоставления двух словарей + dbt-папки), `073-dbt-project-evaluator` (шесть групп
правил, fct_-модели и тесты вместо отчёта, исключения seed-ом),
`068-data-vault-i-automatedv` (hub/link/satellite + макрос AutomateDV),
`069-materialized-views-clickhouse` (MV как триггер на INSERT, AggregatingMergeTree,
GROUP BY = ORDER BY, catchup, refreshable), `070-mikrobatching` (dbt 1.9: event_time,
batch_size, lookback, ретрай батча), `071-dbt-i-potokovye-dannye` (dbt-risingwave:
materialized_view вместо incremental, sink; streaming tables в Spark),
`072-prava-dostupa-v-dbt` (grants как конфиг, плюс = добавить, границы ответственности DBA).
`slides_max` 64 → 72, пины тестов 70/26.

**Upsert инструментов** (по границе контрактов: `.tmp/` — только читающее, мутирующее — в
`scripts/`):
- `.tmp/lint_content_scalars.py` + `just preza-lint` / `just -f .tmp/Justfile lint-scalars` —
  ловит значения контент-YAML, которые YAML разобрал как мапу/число. Сразу нашёл настоящий
  дефект в легаси `preza-dagster-v1-content.yml` (баллы числами в таблице) — закавычено.
- `scripts/preza/edit_slides.py` + `just preza-slides` — list/extract/remove/move/insert по id
  слайда с сохранением форматирования (round-trip проверен байт-в-байт). Заменяет разовые
  splice-скрипты, которыми в этой сессии правили деку трижды.
- Скилл `preza-de-validate`: scalar-gate добавлен в канонический `validate_content.py`
  (**в репозитории ~/.ai, закоммитить отдельно**) и описан в SKILL.md; хардлинк с
  `.claude/skills/` сохранён (inode тот же).
- Сабагент `preza-accents-keeper`: инварианты 6–8 — правки только через `preza-slides`
  (никакого yaml round-trip), `preza-lint` перед билдом, порядок «поднять slides_max с
  датированным комментарием + обновить пины тестов».

**Проверки:** 261 passed, 4 skipped; preza-validate-all — 5 дек чисто (регрессии от нового
гейта нет); preza-review 4/4 акцента. Собрана `v3.19` (70 слайдов), в TG не отправлялась.
