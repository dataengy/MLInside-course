

uvx kedro new --starter spaceflights-pandas --name spaceflights
uv sync
source .venv/bin/activate 
echo "consent: false" > .telemetry

uv run kedro info  
kedro ipython   

```python
companies = catalog.load("companies")
companies[:3]
shuttles = catalog.load("shuttles")
shuttles[:3]
exit()
```
uv pip install kedro-viz
kedro viz run

uv pip install kedro-airflow
kedro airflow create    

uv pip install kedro-docker
kedro docker init 
kedro docker build 

uv pip install --upgrade kedro-mlflow
kedro mlflow init
kedro mlflow ui